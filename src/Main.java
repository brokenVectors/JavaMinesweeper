import Renderer.*;
import Logic.*;
public class Main {
    public static void main(String[] args) {
        // Space to reset
        // LClick to reveal
        // RClick to flag
        Renderer minesweeperFrame = new Renderer(400, 425, 10, 10);
    }
}
